import React from 'react';
import NotAuthorizedLayout from '../layout/NotAuthorizedLayout';

const NotAuthorizedRoute = (): React.ReactElement => {
  return (
    <React.Fragment>
      <NotAuthorizedLayout />
    </React.Fragment>
  );
};

export default NotAuthorizedRoute;
